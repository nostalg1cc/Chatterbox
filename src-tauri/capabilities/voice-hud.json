{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "voice-hud",
  "description": "Minimal permissions for the floating voice HUD - it only receives participant updates pushed from the main window and never calls back into anything privileged.",
  "windows": [
    "voice-hud"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
